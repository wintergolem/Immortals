using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum PreMoveActionType { None, Summon, CheckMap, Retribution }
public enum PostMoveActionType { None, PowerReset }
public enum CaptureReactionType { None, Retribution }